# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_NAME=carpool_db
DB_USERNAME=root
DB_PASSWORD=carpool_password

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8080
CORS_ALLOW_CREDENTIALS=true
CORS_MAX_AGE=3600

# Security Configuration
PASSWORD_STRENGTH=12

# Server Configuration
SERVER_PORT=8080
CONTEXT_PATH=/api

# Logging Configuration
LOG_LEVEL=INFO
APP_LOG_LEVEL=DEBUG
HIBERNATE_LOG_LEVEL=INFO
SPRING_WEB_LOG_LEVEL=INFO

# Supabase Configuration
SUPABASE_API_KEY=
SUPABASE_CODE=
SUPABASE_USERPHOTOS_BUCKET_NAME=

# RabbitMQ Configuration
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest
RABBITMQ_MANAGEMENT_PORT=15672
RABBITMQ_VHOST=/ #Should be empty for default vhost
QUEUE_NOTIFICATIONS="carpool.notifications"
QUEUE_MATCHES="carpool.matches"
QUEUE_RIDE_CREATED="carpool.rides.created"
QUEUE_RIDE_UPDATED="carpool.rides.updated"
QUEUE_REQUEST="carpool.rides.request"
QUEUE_AVALIACAO="carpool.avaliacao"

# Firebase Cloud Messaging (FCM) Configuration
# Instructions:
# 1. Download your Firebase service account credentials from Firebase Console > Project Settings > Service Accounts
# 2. Save the JSON file in a secure location (e.g., in src/main/resources/)
# 3. Set the path to this file in FCM_CREDENTIALS_FILE
# 4. Set FCM_PROJECT_NAME to your Firebase project ID (typically something like 'yourprojectname-xxxxx')
FCM_CREDENTIALS_FILE=src/main/resources/carpool-firebase-adminsdk.json
FCM_PROJECT_NAME=carona-c9eba
FCM_BASE_URL=https://fcm.googleapis.com/v1/
